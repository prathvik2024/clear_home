import 'dart:ui';

class FamilyCardModel{
  String profileImage, personName, personType, familyCategory, taskList;
  Color color;
  FamilyCardModel(this.profileImage, this.personName, this.personType, this.familyCategory, this.taskList, this.color);
}